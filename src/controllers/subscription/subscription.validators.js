import Joi from 'joi';

export default {
    /*createSubscription: {
        body: {
            siteId: Joi.string().required(),
            endpoint: Joi.string().required(),
            keys: Joi.object().required()
        }
    }*/
};
